langcode: en
status: true
dependencies:
  config:
    - group.type.tenant
    - node.type.webform
  module:
    - gnode
    - node
id: tenant-group_node-webform
label: 'Tenant: Group node (Webform)'
description: 'Adds <em class="placeholder">Webform</em> content to groups both publicly and privately.'
group_type: tenant
content_plugin: 'group_node:webform'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
