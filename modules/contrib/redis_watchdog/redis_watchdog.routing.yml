redis_watchdog.logs:
  path: 'admin/reports/redislog'
  defaults:
    _controller: '\Drupal\redis_watchdog\Pages\RedisWatchDogController::redisWatchdogOverview'
    _title: 'Recent Redis Log Messages'
  requirements:
    _permission: 'access site reports'

redis_watchdog.event:
  path: 'admin/reports/redislog/event/{event_id}'
  defaults:
    _form: '\Drupal\redis_watchdog\Pages\RedisWatchDogController::event'
    _title: 'Redis Log Event Details'
   requirements:
    _permission: 'access site reports'

redis_watchdog.type:
  path: 'admin/reports/redislog/type/{type_id}/{page_number}'
  defaults:
    _form: '\Drupal\redis_watchdog\Pages\RedisWatchDogController::type'
    _title: 'Log Type Report'
   requirements:
    _permission: 'access site reports'

redis_watchdog.export:
  path: 'admin/reports/redislog/export'
  defaults:
    _form: '\Drupal\redis_watchdog\Pages\RedisWatchDogController::export'
    _title: 'Export Redis Log Messages'
   requirements:
    _permission: 'access site reports'

redis_watchdog.export-download:
  path: 'admin/reports/redislog/export/download'
  defaults:
    _form: '\Drupal\redis_watchdog\Pages\RedisWatchDogController::download'
    _title: 'File download router'
   requirements:
    _permission: 'access site reports'